import type { Route } from "./+types/route"
import RegisterForm from "./RegisterForm"

const RegisterPage = (_props: Route.ComponentProps) => {
  return <RegisterForm />
}

export default RegisterPage
