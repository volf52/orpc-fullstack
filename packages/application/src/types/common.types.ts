/**
 * Common application types
 *
 * This file contains shared types used across the application layer.
 * Currently empty but reserved for future common types.
 */

// Placeholder export to satisfy linter
export type ApplicationLayerMarker = "application"
